# ETHKyt: The ETH Key-related Toolkit

## Setup
```bash
# npm i tsx -g
npm i
```


## Usage
### metamask derive keys
- copy `test/config-example.ts` to `test/config.ts`, and config it.
- try set `needIndex`, `needAddress`, `needPrivateKey` to customize output format

```bash
npm run dkeys
```
